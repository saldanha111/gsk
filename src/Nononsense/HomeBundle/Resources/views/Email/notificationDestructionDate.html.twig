<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GSK</title>
</head>

<body>
<p>Plantillas cuya fecha de destrucción se alcanzó hace seis meses</p>
{% if templates | length > 0 %}
    <table class="table table-dark">
        <thead>
            <tr>
                <th>Nombre</th>
            </tr>
        </thead>
        <tbody>
            {% for i in range(0, templates | length - 1) %}
            <tr>
                <td>{{ templates[i]["name"] }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
{% endif %}
</body>
</html>

