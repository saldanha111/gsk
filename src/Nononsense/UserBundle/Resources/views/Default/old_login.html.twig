<!DOCTYPE html>
<html>

<head>

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>DOCXPRESSO | Login</title>

    <link href="/css/bootstrap.min.css" rel="stylesheet">
    <link href="/font-awesome/css/font-awesome.css" rel="stylesheet">

    <link href="/css/animate.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">

</head>

<body class="gray-bg">

    <div class="middle-box text-center loginscreen  animated fadeInDown">
        <div>
            <div>

                <h1 class="logo-name"><img src="/img/logo_portada.png" /></h1>

            </div>
            <p>{{'The ultimate tool for managing your document repository, period.'|trans}}
            </p>
            {% if error %}
                <div class="alert alert-danger alert-dismissable" style="text-align: center;">
                    <button aria-hidden="true" data-dismiss="alert" class="close" type="button">×</button>
                    &emsp;&emsp;{{'Bad credentials.'|trans}}
                </div>
            {% endif %}
            <form class="m-t" role="form" action="{{ path("nononsense_user_check") }}" method="post">
                <div class="form-group">
                    <input  name="_username" class="form-control" placeholder="{{'Enter email address'|trans}}" required="">
                </div>
                <div class="form-group">
                    <input type="password" name="_password" class="form-control" placeholder="{{'Password'|trans}}" required="">
                </div>
                <button type="submit" name="_submit" class="btn btn-primary block full-width m-b">{{'Let me in'|trans}}*</button>

                <a href="{{ path('nononsense_user_recover_password') }}"><small>*{{'Forgot your password?'|trans}}</small></a>
                <!--<p class="text-muted text-center"><a href="{{ path('nononsense_user_request_account') }}"><small style="color: #b70000">{{'Do not have an account?'|trans}}</small></a></p>-->
            </form>
            <p class="m-t"> <small>No-nonsense Labs &copy; 2015</small> </p>
        </div>
    </div>

    <!-- scripts -->
    <script src="/js/jquery-2.1.1.js"></script>
    <script src="/js/bootstrap.min.js"></script>

</body>

</html>
