{% set batch = app.session.get('batch')%}

{% set groups = app.user.getGroups    %}
{% set FLL = "0" %}
{% set ECO = "0" %}

{% if groups|length > 0 %}
{% for group in groups %}
{% set type = group.group.tipo %}

{% if type == 'FLL' %}
{% set FLL = "1" %}
{% endif %}
{% if type == 'ECO' %}
{% set ECO = "1" %}
{% endif %}

{% endfor %}
{% endif %}

<nav class="navbar-default navbar-static-side" role="navigation">
    <div class="navbar-header hidden-xs">
        <a class="navbar-minimalize minimalize-styl-2 btn btn-primary " href="#"><i class="fa fa-bars"></i> </a>
    </div>
    <div class="sidebar-collapse">
        <ul class="nav" id="side-menu">
            <li class="nav-header">
                {{ render(controller('NononsenseUserBundle:Default:widget')) }}
            </li>
            <li id="docxpresso_home">
                <a href="{{path('nononsense_home_homepage')}}"><i class="fa fa-home"></i> <span class="nav-label">{{'Home'|trans}}</span></a>
            </li>
            <li id="docxpresso_nuevo">
                <a href="{{path('nononsense_nuevo_contrato')}}?categoriasSelect=0"><i class="fa fa-pencil-square"></i> <span class="nav-label">{{'Nuevo registro'|trans}}</span></a>
            </li>
            <li id="docxpresso_search">
                <a href="{{path('nononsense_search')}}"><i class="fa fa-search" aria-hidden="true"></i> <span class="nav-label">Buscador</span></a>
            </li>
            {% if app.user.id==1 or  app.user.id==2 or  app.user.id==3  or  app.user.id==4  %}
                <li id="docxpresso_search_contains">
                    <a href="{{path('nononsense_search')}}?record_contains=1"><i class="fa fa-search-plus" aria-hidden="true"></i> <span class="nav-label">Buscador de contenido</span></a>
                </li>
            {% endif %}
            <li id="docxpresso_proceso">
                <a href="{{path('nononsense_search')}}?pending_for_me=1"><i class="fa fa-folder"></i> <span class="nav-label">{{'En proceso'|trans}}</span></a>
            </li>
            {% if FLL == 1 %}
            <li id="docxpresso_auth">
                <a href="{{path('nononsense_registro_autorizar_list')}}"><i class="fa fa-magic" aria-hidden="true"></i> <span class="nav-label">{{'Autorizaciones'|trans}}</span></a>
            </li>
            {% endif %}
            {% if FLL == 1 %}
                <li id="docxpresso_proceso">
                    <a href="{{path('nononsense_activity')}}?limit_many=15"><i class="fa fa-pie-chart" aria-hidden="true"></i> <span class="nav-label">Actividad de área</span></a>
                </li>
            {% endif %}
            {% if FLL == 1 %}
                <li id="docxpresso_mydocs">
                <a href="{{path('nononsense_search')}}?destruction=1"><i class="fa fa-trash" aria-hidden="true"></i> <span class="nav-label">Retención y destrucción</span></a>
            </li>
            {% endif %}
            <!--<li id="docxpresso_archivo">
                <a href="{{path('nononsense_registro_archivo')}}"><i class="fa fa-folder"></i> <span class="nav-label">{{'Archivo'|trans}}</span></a>
            </li>-->
            {% if FLL == 1 or ECO == 1 %}

            <li id="docxpresso_standby">
                <a href="{{path('nononsense_backoffice_standby_documents_list')}}"><i class="fa fa-exclamation-triangle"></i> <span class="nav-label">{{'Documentos Stand By'|trans}}</span></a>
            </li>
            {% endif %}

            <li id="docxpresso_plantdocs">
                <a href="{{path('nononsense_documents')}}"><i class="fa fa-cogs" aria-hidden="true"></i> <span class="nav-label">Plantillas Doc.</span></a>
            </li>
            <li id="docxpresso_docs">
                <a href="{{path('nononsense_records')}}"><i class="fa fa-file-text" aria-hidden="true"></i> <span class="nav-label">Documentos</span></a>
            </li>
            <li id="docxpresso_mydocs">
                <a href="{{path('nononsense_records')}}?pending_for_me=1"><i class="fa fa-file" aria-hidden="true"></i> <span class="nav-label">Mis Doc. Pend.</span></a>
            </li>

            {% if 'plantillas_contratos_gestion' in array_subsecciones or 'contratos_gestion' in array_subsecciones %}
                <li>
                    <a href="#">
                        <i class="fa fa-file-text" aria-hidden="true"></i>
                        <span class="nav-label">Contratos</span>
                        <span class="fa arrow"></span>
                    </a>
                    <ul class="nav nav-second-level">
                        {% if 'plantillas_contratos_gestion' in array_subsecciones %}
                            <li id="li_plantillas_contratos"><a href="{{path('nononsense_contracts')}}"><i class="fa fa-cogs" aria-hidden="true"></i> <span class="nav-label">Plantillas Contratos</span></a></li>
                        {% endif %}
                        {% if 'contratos_gestion' in array_subsecciones %}
                            <li id="li_contratos"><a href="{{path('nononsense_records_contracts')}}"><i class="fa fa-file-text" aria-hidden="true"></i> <span class="nav-label">Contratos</span></a></li>
                        {% endif %}
                    </ul>
                </li>
            {% endif %}


            
            {% if 'productos_gestion' in array_subsecciones or 'productos_recepcion' in array_subsecciones or 'productos_retirada' in array_subsecciones %}
                <li>
                    <a href="#">
                        <i class="fa fa-medkit" aria-hidden="true"></i>
                        <span class="nav-label">{{'Reactivos'|trans}}</span>
                        <span class="fa arrow"></span>
                    </a>
                    <ul class="nav nav-second-level">
                        {% if 'productos_gestion' in array_subsecciones %}
                            <li id="browse_products"><a href="{{path('nononsense_products')}}">{{'Productos'|trans}}</a></li>
                        {% endif %}
                        {% if 'productos_recepcion' in array_subsecciones %}
                            <li id="browse_product_inputs"><a href="{{path('nononsense_products_inputs')}}">{{'Recepción de material'|trans}}</a></li>
                        {% endif %}
                        {% if 'productos_retirada' in array_subsecciones %}
                            <li id="browse_product_outputs"><a href="{{path('nononsense_products_outputs')}}">{{'Retirada de material'|trans}}</a></li>
                        {% endif %}
                    </ul>
                </li>
            {% endif %}
            
            {% if 'mc_materials_list' in array_subsecciones or 'mc_centers_list' in array_subsecciones or 'mc_codes_list' in array_subsecciones %}
            <li>
                <a href="#">
                    <i class="fa fa-shower" aria-hidden="true"></i>
                    <span class="nav-label">{{'Material Limpio'|trans}}</span>
                    <span class="fa arrow"></span>
                </a>
                <ul class="nav nav-second-level">
                    {% if 'mc_materials_list' in array_subsecciones %}
                        <li id="admin_materials"><a href="{{path('nononsense_mclean_materials_list')}}">{{'Listado de Materiales'|trans}}</a></li>
                    {% endif %}
                    {% if 'mc_centers_list' in array_subsecciones %}
                        <li id="admin_centers"><a href="{{path('nononsense_mclean_centers_list')}}">{{'Listado de Centros'|trans}}</a></li>
                    {% endif %}
                    {% if 'mc_codes_list' in array_subsecciones %}
                        <li id="admin_codes"><a href="{{path('nononsense_mclean_codes_list')}}">{{'Generación de Códigos de barras'|trans}}</a></li>
                    {% endif %}
                    {% if 'mc_cleans_scan' in array_subsecciones %}
                        <li id="admin_cleans"><a href="{{path('nononsense_mclean_cleans_scan')}}">{{'Limpieza de material'|trans}}</a></li>
                    {% endif %}
                    {% if 'mc_uses_scan' in array_subsecciones %}
                        <li id="admin_uses"><a href="{{path('nononsense_mclean_uses_scan')}}">{{'Utilización de material'|trans}}</a></li>
                    {% endif %}
                    {% if 'mc_traces_list' in array_subsecciones %}
                        <li id="admin_traces"><a href="{{path('nononsense_mclean_traces_list')}}">{{'Trazabilidad'|trans}}</a></li>
                    {% endif %}
                </ul>
            </li>
            {% endif %}
            {% if 'qrs_gestion' in array_subsecciones or 'areas_gestion' in array_subsecciones or 'usuarios_gestion' in array_subsecciones  or 'grupos_gestion' in array_subsecciones %}
                <li>
                    <a href="#"><i class="fa fa-fw fa-gear"></i> <span class="nav-label">{{'Configuración'|trans}}</span><span
                            class="fa arrow"></span></a>
                    <ul class="nav nav-second-level">
                        {% if 'qrs_gestion' in array_subsecciones %}
                            <li id="qr_list">
                                <a href="{{path('nononsense_qr_list')}}"><span class="nav-label">QRs</span></a>
                            </li>
                        {% endif %}
                        {% if 'areas_gestion' in array_subsecciones %}
                            <li id="areas_list">
                                <a href="{{path('nononsense_areas')}}"><span class="nav-label">Areas</span></a>
                            </li>
                        {% endif %}
                        {% if 'grupos_gestion' in array_subsecciones %}
                            <li id="browse_groups">
                                <a href="{{path('nononsense_groups_homepage')}}"><span class="nav-label">Grupos</span></a>
                            </li>
                        {% endif %}
                        {% if 'usuarios_gestion' in array_subsecciones %}
                            <li id="browse_users">
                                <a href="{{path('nononsense_users_homepage')}}"><span class="nav-label">Usuarios</span></a>
                            </li>
                        {% endif %}
                    </ul>
                </li>
             {% endif %}
        </ul>
    </div>
</nav>