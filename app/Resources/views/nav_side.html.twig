{% set batch = app.session.get('batch')%}

<nav class="navbar-default navbar-static-side" role="navigation">
    <div class="navbar-header hidden-xs">
        <a class="navbar-minimalize minimalize-styl-2 btn btn-primary " href="#"><i class="fa fa-bars"></i> </a>
    </div>
    <div class="sidebar-collapse">
        <ul class="nav" id="side-menu">
            <li class="nav-header">
                {{ render(controller('NononsenseUserBundle:Default:widget')) }}
            </li>
            <li id="docxpresso_home">
                <a href="{{path('nononsense_home_homepage')}}"><i class="fa fa-home"></i> <span class="nav-label">{{'Home'|trans}}</span></a>
            </li>
            <li id="docxpresso_nuevo">
                <a href="{{path('nononsense_nuevo_contrato')}}"><i class="fa fa-pencil-square"></i> <span class="nav-label">{{'Nuevo registro'|trans}}</span></a>
            </li>
            <li id="docxpresso_proceso">
                <a href="{{path('nononsense_registro_enproceso')}}"><i class="fa fa-folder"></i> <span class="nav-label">{{'En proceso'|trans}}</span></a>
            </li>
            <li id="docxpresso_archivo">
                <a href="{{path('nononsense_registro_archivo')}}"><i class="fa fa-folder"></i> <span class="nav-label">{{'Archivo'|trans}}</span></a>
            </li>
            <li id="docxpresso_archivo">
                <a href="#"><i class="fa fa-file-text" aria-hidden="true"></i> <span class="nav-label">Documentos</span></a>
            </li>
            <li id="docxpresso_archivo">
                <a href="#"><i class="fa fa-file" aria-hidden="true"></i> <span class="nav-label">Doc. pendientes</span></a>
            </li>
            <li>
                <a href="#"><i class="fa fa-users"></i> <span class="nav-label">{{'Users'|trans}}</span><span
                        class="fa arrow"></span></a>
                <ul class="nav nav-second-level">
                    <li id="browse_users"><a href="{{path('nononsense_users_homepage')}}">{{'Browse users'|trans}}</a>
                    </li>
                    <li id="browse_groups"><a href="{{path('nononsense_groups_homepage')}}">{{'Browse groups'|trans}}</a></li>
                    {% if app.user and is_granted('ROLE_ADMIN') %}
                    <li id="create_user"><a href="{{path('nononsense_user_create')}}">{{'Create user'|trans}} <span class="label label-primary pull-right">{{'+'|trans}}</span></a></li>
                    <li id="create_group"><a href="{{path('nononsense_group_create')}}">{{'Create group'|trans}} <span class="label label-primary pull-right">{{'+'|trans}}</span></a></li>
                    {% endif %}
                </ul>
            </li>
            {% if app.user and is_granted('ROLE_ADMIN') %}
            <li id="docxpresso_admin">
                <a href="#"><i class="fa fa-cogs"></i> <span class="nav-label">{{'Administracion'|trans}}</span><span
                        class="fa arrow"></span></a>
                <ul class="nav nav-second-level">
                    <li id="browse_verification_groups"><a href="{{path('nononsense_backoffice_verificator_groups')}}"><i class="fa fa-users"> </i> {{'Gestionar grupos verificacion'|trans}}</a></li>
                </ul>
            </li>
            {% endif %}


        </ul>
    </div>
</nav>